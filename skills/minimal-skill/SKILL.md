---
name: Minimal Skill
description: A skill with only required fields populated
---

# Minimal Skill

This skill has only the required `name` and `description` frontmatter fields. Used to verify that the scanner handles minimal metadata gracefully.
