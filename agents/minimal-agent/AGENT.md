---
name: Minimal Agent
description: An agent with only required fields populated
---

# Minimal Agent

You are a helpful assistant. This agent has only the required `name` and `description` frontmatter fields.
